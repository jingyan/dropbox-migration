[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/docker-entrypoint.sh migrate"
restartPolicyType = "NEVER"
# CLI batch job — no HTTP server
healthcheckPath = ""
healthcheckTimeout = 1
